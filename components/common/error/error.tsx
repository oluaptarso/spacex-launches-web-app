const APIDefaultError: React.FC = () => {
  return (
    <span className="error">
      Oops, there was an error communicating with the API, please try again
      later.
    </span>
  );
};

export default APIDefaultError;
